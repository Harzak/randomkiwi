<?xml version="1.0" encoding="utf-8" ?>
<Grid x:Class="randomkiwi.Views.BookmarksView"
      xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
      xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
      xmlns:pullToRefreshControl="clr-namespace:Syncfusion.Maui.Toolkit.PullToRefresh;assembly=Syncfusion.Maui.Toolkit"
      xmlns:res="clr-namespace:randomkiwi.Resources"
      xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
      xmlns:viewModels="clr-namespace:randomkiwi.ViewModels"
      x:DataType="viewModels:BookmarksViewModel">

    <pullToRefreshControl:SfPullToRefresh x:Name="pullToRefresh" TransitionMode="Push">
        <pullToRefreshControl:SfPullToRefresh.PullableContent>
            <Grid>
                <CollectionView ItemsSource="{Binding Bookmarks}" SelectionMode="Single">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="viewModels:BookmarkModel">
                            <Grid Margin="10,5" Padding="10"
                                  BackgroundColor="White">

                                <Border BackgroundColor="White" Stroke="Transparent" StrokeThickness="0">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="10" />
                                    </Border.StrokeShape>
                                    <Border.Shadow>
                                        <Shadow Brush="Gray" Opacity="0.3" Radius="5"
                                                Offset="2,2" />
                                    </Border.Shadow>

                                    <Grid Padding="10"
                                          ColumnDefinitions="*,44" RowDefinitions="Auto,Auto,Auto">

                                        <Label Grid.Row="0" Grid.Column="0"
                                               Margin="10,0,0,5"
                                               FontAttributes="Bold" FontSize="16"
                                               Text="{Binding Name}" />

                                        <Label Grid.Row="1" Grid.Column="0"
                                               Margin="10,0,0,5"
                                               FontSize="13"
                                               Text="{Binding Description}" />

                                        <Label Grid.Row="2" Grid.Column="0"
                                               Margin="10,0,0,0"
                                               FontAttributes="Bold" FontSize="12" Opacity="0.7"
                                               Text="{Binding DateAdded, StringFormat='{}{0:d}'}" />


                                        <ImageButton Grid.Row="0" Grid.RowSpan="3" Grid.Column="1"
                                                     Margin="5,0"
                                                     Aspect="Center"
                                                     Command="{Binding Source={RelativeSource AncestorType={x:Type Grid}}, Path=BindingContext.OpenBookmarkCommand}"
                                                     CommandParameter="{Binding .}"
                                                     HeightRequest="44" HorizontalOptions="End"
                                                     Source="{StaticResource IconView}"
                                                     VerticalOptions="Center" WidthRequest="44" />

                                    </Grid>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>

                    <CollectionView.EmptyView>
                        <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
                            <Label FontSize="18"
                                   Text="{x:Static res:Languages.NoBookmarksAvailable}"
                                   TextColor="Gray" />
                        </StackLayout>
                    </CollectionView.EmptyView>

                </CollectionView>
            </Grid>
        </pullToRefreshControl:SfPullToRefresh.PullableContent>
    </pullToRefreshControl:SfPullToRefresh>
</Grid>